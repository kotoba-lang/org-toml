{
  "name": "org-toml",
  "private": true,
  "description": "TOML v1.0.0 reader in portable .cljc",
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljs"
  }
}
